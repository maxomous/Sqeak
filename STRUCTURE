




GRBL::


	Connect()
	Disconnect()
	
	
	
	SendRTCmd()
	
	SendFile()		running = true
	SendCmd()		running = true
	SendJog()		running = true
	
		GRBL::Send()		tests:	isConnected()	isRunning()
		
			adds to gcList
		
	Cancel() - stopRunning (clears any unsent commands but ones left in grbl's buffer still exectute)
